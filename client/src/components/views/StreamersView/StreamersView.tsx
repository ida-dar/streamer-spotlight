import SubmissionView from '../SubmissionView/SubmissionView';
import StreamerRecord from '../StreamerRecord/StreamerRecord';

const StreamerRecords = () => {
  return (
    <div>
      <SubmissionView />
      <StreamerRecord />
    </div>
  );
};

export default StreamerRecords;
